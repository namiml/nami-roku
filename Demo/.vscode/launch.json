{
  "configurations": [
    {
      "host": "${promptForHost}",
      "name": "BrightScript Debug: Launch",
      "password": "${promptForPassword}",
      "request": "launch",
      "rootDir": "${workspaceFolder}",
      "type": "brightscript"
    }
  ],
  "version": "0.2.0"
}
